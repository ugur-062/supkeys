import { FirmaProfiliView } from "../_components/firma-profili-view";

export const metadata = {
  title: "Firma Profili — Supkeys",
};

export default function FirmaProfiliPage() {
  return <FirmaProfiliView />;
}
