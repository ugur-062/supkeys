import { OnboardingClient } from "./_components/onboarding-client";

export const metadata = { title: "Firma Doğrulama — Rothern" };

export default function Page() {
  return <OnboardingClient />;
}
