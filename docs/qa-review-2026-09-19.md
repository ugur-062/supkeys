# Uçtan uca sistem incelemesi — 2026-09-19

Kapsam: kullanıcı talebi ("her tuşu dene; frontend, backend ve gerçek iş akışı
mantığı"). Ortam: staging (`staging.supkeys.com`, API `api.staging.supkeys.com`).
Hesaplar: Demo Gold Makina (kurucu + roller), QA alıcı/tedarikçi/ücretsiz
firmaları, admin. Bulgu ciddiyeti: **K** kırık · **Y** yanlış davranış ·
**İ** iş mantığı · **U** kullanılabilirlik.

Ön iş: detaylı sihirbaz kaldırıldı (tek giriş hızlı kart; kopya / AI belge /
şablon / düzenleme aynı karta indi) — `dee0ca2d`.

## 1. Otomatik paket (staging e2e + API birim)

Koşum: `e2e-staging.sh --workers=1` (2026-09-19 16:45, staging = `819ab9b6`).
İlk koşumda üç sistemik neden görüldü, ikisi düzeltildi:

- **K-1 (test verisi, düzeltildi `f3b304d9`):** 9 spec talep kategorisi olarak
  `10101500` kullanıyordu; segment 10 (canlı bitki/hayvan) 2026-09-19'da
  `HIDDEN_SEGMENTS` ile gizlenince `validateListingBusinessRules` "Geçersiz
  kategori seçimi" (400) döndü → sipariş zinciri, onay akışı, pazarlık, para,
  eşzamanlılık, rol-teklif, e-posta içeriği, kiracı yalıtımı, satış zinciri
  kırmızı. Kategori `31161500` (Vidalar) oldu. API entegrasyon testleri aynı
  kodu **factory** ile yazdığı için (doğrulama yolu yok) CI yeşil kaldı.
- **Y-2 (ürün hatası, düzeltildi `f3b304d9`):** giriş ekranı hız sınırında ham
  `ThrottlerException: Too Many Requests` basıyordu (İngilizce, teknik).
  `ThrottlerModule` `errorMessage` → "Çok fazla deneme yapıldı. Lütfen bir
  dakika bekleyip yeniden deneyin." (tek kaynak `common/http/throttle-message.ts`).
  Koşumda görünmesinin nedeni ayrı: rol-giriş testleri aynı IP'den art arda
  giriş yapıyor; `uiLogin` 429'da bekleyip yineliyor, ekran yine de metni gösterdi.
- **K-2 (tasarım kararı bekliyor):** yeni marka mavisi `#2F9BF2` üstünde beyaz
  yazı **2,96:1** (WCAG AA 4,5:1 ister); `#1F86DA` hover/aktif metin açık mavi
  zeminde 3,53:1. Yeşil `#1FB864` üstünde beyaz 2,59:1. axe `color-contrast`
  **serious** → `staging-a11y` kapısı kırmızı (7 düğüm `/company/satinalma`).
  Seçenekler: (a) dolgu ve metin rengi için bir ton koyu (`#1B74C9` → 4,79:1 ·
  yeşil `#128A47` → 4,42, `#0F7A3E` → 5,42), açık tonlar çip/ikon/zeminde
  kalır; (b) rengi koruyup kapıyı gevşetmek (önerilmez — görme zorluğu olan
  kullanıcı düğme yazısını okuyamaz, nightly her gece konu açar).

## 2. Kayıt ve hesap

- `staging-signup` (kayıt → e-posta kodu → onboarding → panel → hesap silme) bu
  koşumda yeşil; e-posta içerik testi yalnız kategori nedeniyle düştü.
- Giriş hız sınırı metni Y-2 (§1) — düzeltildi.
- Şifre sıfırlama / davetle katılım / 2FA akışları otomatik pakette
  (`staging-role-logins`, `staging-signup`), elle yinelenmedi.

## 3. Rol ve yetki denetimi

- `staging-role-matrix` (11 rol × 62 uç), `staging-role-screens` (8 rol × 23
  sayfa), `staging-role-writes` (boş gövde yazma matrisi), `staging-tenant-
  isolation` (firmalar arası yalıtım) yeşil; üretilen `docs/qa-role-matrix.md`
  ve `docs/qa-role-screens.md` bir önceki koşumla **birebir** (diff yok) →
  bu turdaki değişiklikler (sihirbaz kaldırma, tema, kategori gizleme) yetki
  yüzeyini değiştirmedi.
- Elle: Demo Gold onaylayıcı yalnız Onaylar'ı görüyor, talep sayfası "yetki
  gerektirir" (§7) ✓. Admin SUPPORT/SALES kısıtları §11'de.
- API sondası IDOR/CSRF/paket kapıları §12'de ✓.

## 4. Satınalma akışı (alıcı)

Demo Gold kurucu, tarayıcıdan (`walk-buyer`), 2026-09-19 17:00:
- Yeni talep sayfası tek giriş (detaylı sihirbaz bağlantısı yok ✓). Kalem satırı,
  Katalogdan Ekle, Excel ile İçe Aktar, Belgeden Doldur diyalogları açılıyor ✓.
- "AI ile başlık ve kategori bul": başlık "S235 kalitesinde çelik levha alımı",
  kategori "Temel çelikler" (L3) + öneri çipleri ✓ (~10 sn).
- Kapanış çipleri 3·7·14, adres ekleme, "Kimler görsün?" seçici (2 satır,
  kalem ilgisine göre sıralı) ✓. "Şablon olarak kaydet" → diyalog + toast ✓.
- AI tedarikçi keşfi modalı: Platformda sekmesi (1 firma, "Bağlantı daveti
  gönder"); "Web'de Ara (AI)" sekmesi bölge alanı + düğme ✓.
- Yayın → talep sayfası; Gelen Teklifler tezgâhı, "AI ile tedarikçi bul" ✓.
- **İ-4 (karar bekliyor):** Kapsam ikili — "Yurtiçi" yalnız aynı ülke,
  "Uluslararası" yalnız **yabancı** ülkeler görür (`sellerVisibleWhere`,
  `targetCountries` kendi ülkesini süzer). Demo Gold'un profili "Uluslararası"
  olduğu için yayınlanan talebi hiçbir TR tedarikçi göremedi (QA tedarikçi
  404). Kural bilinçli (Incoterm/ödeme simetrisi) ama etiket bunu söylemiyor;
  alıcı "uluslararası = herkese açık" sanıyor. Öneri: etiketi "Yalnız yurt
  dışı" yapmak ya da üçüncü seçenek "Yurtiçi + yurt dışı" (Incoterm yalnız
  yabancı tedarikçiye uygulanır) eklemek. Yürüyüş için demo şartları yurtiçine
  çevrildi.
- **U-1:** AI keşfi modalındaki "Platformda / Web'de Ara (AI)" sekmeleri
  `role="tab"` taşımıyor (düz düğme); klavye/okuyucu için sekme listesi değil.

## 5. Satış akışı (tedarikçi)

QA tedarikçi satışçı (Silver), tarayıcıdan (`walk-seller`, 17:16):
- Açık Talepler: 20 satır, sıralama çipleri (Size uygun / Yakın biten / Uzak
  biten / En yeni), "Firma" pili → 12 firma kartı ✓.
- Talep detayı: Teklif Ver, Paylaş, Kalemler | Dosyalar sekmeleri ✓.
- Teklif formu: kalem başına birim fiyat, geçerlilik, teslim süresi, belge
  alanı; onay diyaloğu toplamı ve "düzenlenemez / geri çekilemez" uyarısını
  gösteriyor ✓ → "Teklifiniz gönderildi"; ikinci teklif düğmesi yok (kilit),
  "Teklifim" kutusu sekmelerin üstünde ✓. Tekliflerim: 10 satır, süzgeçler ✓.

## 6. Pazarlık (eksiltme)

Demo Gold kurucu + QA tedarikçi (1 ve 2), (`walk-negotiate`, 17:18):
- Talep sayfasında görünür eylemler: AI ile tedarikçi bul · Kapanış Zamanını
  Değiştir · **Pazarlığa Geç** · Değerlendirmeye Al · Kalem-bazlı Kazandır ✓.
- "Pazarlık Aşamasına Geç" diyaloğu (kapanış tarih+saat) → "Pazarlık turu
  açıldı"; format ENGLISH_AUCTION, tur 2, iki teklif otomatik taşındı ✓;
  "Yeni Tur Aç" düğmesi belirdi ✓.
- Tedarikçi ekranı: "Pazarlık (Eksiltme)" bandı, "Yalnızca kendi sıranızı
  görürsünüz", rakip firma adı **sızmıyor** ✓; "Yeni Teklif Ver" → formda
  "Öncekinden düşük olmalı" ipucu ✓ → gönderildi.
- Turda ikinci gönderim 400 "Bu turdaki teklifinizi verdiniz — ilan sahibi yeni
  tur açarsa güncelleyebilirsiniz" (BAFO kuralı) ✓; tedarikçi-2 daha düşük 201.
- Alıcı tezgâhı "Gelen Teklifler (2) · Tur 2 · Yayında — otomatik güncellenir",
  "En iyi" rozeti tek ✓; Yeni Tur diyaloğu açılıyor ✓.
- **İ-6 (bilgi):** pazarlık turu açıkken "Kazandır" düğmeleri aktif — alıcı
  tur bitmeden kazandırabilir. Bilinçliyse kalsın.

## 7. Onay akışları

Demo Gold: kurucu akış tanımlar (UI), kurucu kazandırır, onaylayıcı karar verir (`walk-approval`, 17:12):
- Onaylar › "Onay akışlarını düzenle" → "İlk Akışı Oluştur" → 3 adımlı sihirbaz
  (ad → onaycı ekle: seçici Kurucu/Yönetici/Onaylayıcı + bütçe eşiği → özet →
  "Kaydet ve Aktifleştir") ✓ toast "Akış kaydedildi ve aktifleştirildi"; kartta
  Pasifleştir · Düzenle · Kopyala · sil ✓.
- Kazandır → diyalog "Kazandırmayı onaya gönder" (sipariş şimdi oluşmaz uyarısı,
  not alanı) ✓ → talep `IN_AWARD_APPROVAL`; alıcı "Tüm istekler"te İptal et ✓.
- Onaylayıcı: Sıra sizde kartı, "Detay — kazanan, rekabet, kalemler" ✓; talep
  sayfası kendisine kapalı ("yetki gerektirir") ✓ dar bağlam; Onayla diyaloğu
  "Bu son adım — işlem hemen uygulanır" → toast "Onaylandı" → talep AWARDED,
  sipariş `ROT-ORD-000142` PENDING ✓. Akış sonda PASIVE'e çekildi.
- Ret yolu e2e'de (`staging-approval-flow`), bu turda elle koşulmadı.

## 8. Sipariş zinciri

İki taraf tarayıcıdan (aynı koşum):
- Alıcı PENDING: Siparişi İptal Et · Yazdır/PDF ✓. Satıcı PENDING: Reddet ·
  Kabul Et → "Siparişi Onayla" diyaloğu ödeme hesabı seçimi zorunlu (Ayarlar ›
  Banka Hesapları'ndan) ✓ → "Sipariş onaylandı".
- Satıcı ACCEPTED: İptal Talebi · "Siparişi Tamamla" (fatura no zorunlu, kargo
  bilgisi opsiyonel) → "Sipariş gönderildi" ✓. Alıcı IN_DELIVERY: "Teslim
  Aldım" → DELIVERED; vadeli ödemede sipariş otomatik COMPLETED (kabul ≠ ödeme,
  belgeli karar) ✓; DELIVERED'da Ayıp İhbarı · Ödemeyi Yaptım · Değerlendir ✓.
- Ödeme (`walk-payment`): "Ödemeyi Yaptım" diyaloğu (tutar + "Tümünü Öde" +
  not) → bekleyen 36.000 ₺, kalan 0; satıcıya "Ödeme bildirildi — onayı
  bekleniyor" ✓. Satıcı onayı/ret'i bir sonraki koşumda.
- **U-2 (düzeltildi):** "Ödemeyi Yaptım" düğmesi satınalmada SİYAH kalmıştı
  (elle `bg-zinc-900`) → portal rengi.
- Değerlendirme kartı COMPLETED'da iki tarafta da var; "Değerlendir" yıldız
  seçilene dek pasif ✓.

## 9. Ürün vitrini ve moderasyon

Demo Gold ürünleri + admin kuyruğu (`walk-admin`, 17:22):
- Yayındaki ürünün açıklaması değişince PENDING'e düşüp vitrinde kalıyor ✓;
  incelemedeyken ikinci düzenleme 409 `PRODUCT_IN_REVIEW` ✓.
- Admin › Ürünler: süzgeç çipleri, satır seçimi, "İncele" → detay; "Düzeltmeye
  gönder" gerekçe diyaloğu → toast "Düzeltmeye gönderildi, firma bilgilendirildi";
  firma tarafı REJECTED + `rejectReason` ✓; "Onayla ve yayınla" ✓.
- **K-3 (düzeltildi `c99f3186`) — VERİ KAYBI:** `PATCH company/items/:id/showcase`
  gövdeyi TAM vitrin sayıyordu; yalnız `description` gönderen istek görselleri,
  anahtar kelimeleri, nitelikleri, video/belgeyi ve fiyatı sıfırlıyordu → ürün
  "En az 1 görsel eklenmeli" diye yeniden yayınlanamıyordu (staging'de iki demo
  ürün böyle boşaldı, seed'den geri yazıldı). Web formu her alanı gönderdiği için
  ekranda görünmüyordu; API sözleşmesi (asistan/AI/ileride mobil) için tehlikeli.
  Artık `undefined` = dokunma, `null`/`[]` = bilinçli silme (`showcase-merge.ts`,
  birim testi).
- **U-4:** admin "Düzeltmeye gönder" diyaloğunda düğme 4 karakterlik gerekçeyle
  de aktif; ≥10 karakter kuralı sunucuda. (Küçük — diyalog `minLength`i
  düğmeye bağlanabilir.)

## 10. Bilgi talepleri, mesajlar, bağlantılar, bildirimler

## 11. Admin paneli

Süper admin ile 16 rota (`walk-admin`): dashboard, başvurular (1), firmalar
(25 satır), ürünler (5 bekleyen), şikayetler (1), kategoriler, personel (2),
duyuru, e-posta kayıtları (20), denetim kaydı (50), güvenlik (17), sistem
sağlığı (12), üyelik raporu, ayarlar → hepsi 200, hata metni ve 5xx yok.
`/admin/ilanlar` ve `/admin/siparisler` liste sayfası YOK (404) — bilinçli:
yalnız `[id]` detayları var, firma detayının Talepler/Siparişler sekmelerinden
açılır (menüde de yok).
Firma detayı sekmeleri, personel/roller ve SUPPORT kısıtları: `staging-admin`
e2e'de (Destek rolü firma detayına giremez, kategoriler/ürünler okur) yeşil;
bu turdaki elle SUPPORT gezintisi betik hatası nedeniyle tamamlanmadı.

## 12. Backend sözleşmesi ve güvenlik

API sondası (`api-probe`, staging, 2026-09-19 16:45):
- CSRF: başlıksız mutasyon 403, oturumsuz 401 ✓
- IDOR: başka firmanın siparişi / kullanıcı izinleri / adresi → 404 (varlık sızdırılmıyor) ✓
- Doğrulama: boş gövde ve hatalı alanlar 400 + alan bazlı Türkçe mesaj; bilinmeyen alan reddi (`forbidNonWhitelisted`) ✓
- Herkese açık talep listesi/detayı: alıcı adı, hedef fiyat, teklifler, notlar, cuid yok; kalem adları var ✓
- Paket kapıları (ücretsiz): bağlantı daveti 403, AI arama 403, talep açma 403 ✓
- **Bulgu Y-1 (düzeltildi):** boş gövdede `title` için mesaj "Başlık en fazla 200 karakter" çıkıyordu — alan eksikken uzunluk mesajı yanıltıcı. `main.ts` doğrulama eşleyicisi artık alan yoksa "Bu alan zorunlu", varsa önce tip/küme, en son biçim kısıtını basar.

## 13. Şirketim, raporlar, ayarlar

## 14. Herkese açık yüzey ve SEO

## 15. İş akışı mantığı (kaynak kod okuması)

- **Y-3 (düzeltildi `e513da12`):** geçerlilik süresi dolmuş teklif
  kazandırılabiliyordu — ekran "Geçerlilik doldu" rozeti basıyor, sunucu
  `award`/`awardByItem` yalnız `SUBMITTED` kontrol ediyordu → tedarikçinin artık
  bağlı olmadığı fiyata sipariş doğuyordu. Sunucu 400 ("süre uzatması isteyin
  ya da yeni tur açın"), düğme pasif + ipucu. Pazarlıkta geçerlilik süresiz
  (null) → etkilenmez.
- **İ-1 (karar bekliyor):** satıcı siparişi REDDEDİNCE talep AWARDED kalır,
  kazanan teklif WON kalır, diğer teklifler LOST; un-award yok → alıcı aynı
  talepte ikinci tedarikçiye dönemez, yeni talep açmak zorunda. Gerçek hayatta
  ret sık (stok/fiyat değişti). Öneri: ret anında talebi IN_AWARD'a döndürüp
  reddeden teklifi LOST'a çekmek (diğer SUBMITTED teklifler durur) — ya da en
  azından alıcı bildiriminde "Talebi kopyala" bağlantısı.
- **İ-2 (bilgi):** kazandırma kapanış tarihinden ÖNCE de yapılabiliyor
  (`status ∈ {OPEN, IN_AWARD}`); kapalı zarfta erken kazandırma diğer davetlileri
  teklif vermeden eler. İstenen davranışsa kalsın; değilse OPEN'da yalnız
  "Kapat ve değerlendir" sunulmalı.
- Sipariş reddi gerekçe ≥10 karakter (sunucu + ekran aynı) ✓; kazandırma
  VERIFIED ister ✓; belge zorunlu talepte belgesiz teklif kazandırılamaz ✓;
  onay eşiği TRY'ye normalize (yabancı para baypası yok) ✓.

## Bulgular (ciddiyete göre)

| # | Ciddiyet | Alan | Bulgu | Durum |
|---|----------|------|-------|-------|
