# Sistem Baştan Aşağı Denetim — 2026-07-09

Tüm bulgular burada toplanıyor; düzeltmeler denetim bittikten sonra toplu yapılacak (kullanıcı kararı).
Ciddiyet: HIGH = yayın öncesi zorunlu · MED = önemli · LOW = backlog.

## DALGA 2-D DURUMU — Veri/şema + WS (H1,H2,H3,K1) DÜZELTİLDİ ✅ (2026-07-09)

| # | Fix | Dosya |
|---|-----|-------|
| H1 | Company→Order FK CASCADE→RESTRICT (firma siparişleri varken hard-delete engellenir) | schema.prisma + migration `_order_company_restrict` |
| H2 | CompanyConnection yön-bağımsız partial unique index (LEAST/GREATEST, PENDING+ACTIVE) — çift-yön bağlantı DB-seviyesinde engellendi; accept reorder (önce ters-sil sonra ACTIVE) | migration `_connection_unordered_unique` + company-connections.service.ts |
| H3 | CompanyOrder.currency String→Currency enum (USING cast) + group tipi Currency | schema.prisma + migration `_order_currency_enum` + company-listings.service.ts |
| K1 | WS subscribe erişim kontrolü: order→katılımcı, listing→sahip/teklifçi/davetli/bağlantılı (PrismaService enjekte) | realtime.gateway.ts |

**Migration pratiği:** reset'siz (0_init drift'i nedeniyle) manuel SQL + `prisma db execute` + `migrate resolve --applied` (D3 ile aynı). Önce ters-yön çift-bağlantı yokluğu doğrulandı (DO block). H2 fonksiyonel index Prisma şemasında ifade edilemez → yalnız migration (prod); test şeması db push'ta yok, app-katmanı korur.

**K1 kalıntı (kabul):** teklif VERMİŞ rakip, ilanına abone olup kendi ilişkisinden diğer teklif ping'lerini görebilir (dar kalıntı); ilgisiz/engellenen firma tamamen engellendi. Tam kapatma = visibility-farkında ping targeting (auction live-update'i bozmadan) ayrı iş.

Doğrulama: typecheck + build (5/5) + connections (22) + order-workflow/orders/listings (55) yeşil.

**Dalga 2 TAMAMLANDI** (2-A para-yolu + 2-B izolasyon + 2-C altyapı + 2-D veri/şema). Tek atlanan: G-M3 (presigned POST gerekli, ayrı iş).

---

## DALGA 2-C DURUMU — Altyapı (G-M1,G-M2,G-M4,G-M5,EF-M1) DÜZELTİLDİ ✅ (2026-07-09)

| # | Fix | Dosya |
|---|-----|-------|
| G-M1 | Cron overlap çift-email: 4 noktada atomik claim (koşullu updateMany + count) — closeExpired, sendClosingReminders, downgradeExpired, remindPending | listing.scheduler.ts, membership.scheduler.ts, company-approvals.service.ts |
| G-M2 | Hard-bounce/complaint suppression: send() öncesi EmailLog kontrolü, suppress + FAILED kayıt (migration'sız) | email.service.ts |
| G-M4 | /health/storage AdminJwtAuthGuard (iç bilgi sızıntısı) + DB ping 5s timeout | health.controller.ts |
| G-M5 | Resend webhook @SkipThrottle (svix imza korumalı, toplu gönderim 429'u önlenir) | resend-webhook.controller.ts |
| EF-M1 | Admin KPI server-side count/groupBy (stats endpoint + hook), dashboard 200-limitten bağımsız | admin-companies.service/controller.ts, use-admin-companies.ts, dashboard/page.tsx |

**G-M3 ERTELENDİ:** presigned PUT native boyut sınırı desteklemez → presigned POST content-length-range gerekli (frontend+backend, ayrı iş). Mevcut register adımı saklanan nesneyi zaten doğruluyor+siliyor.

Doğrulama: typecheck (api+admin) + build (5/5) + approvals/membership/invitations testleri (24) yeşil.

**Kalan Dalga 2:** veri/şema (H1,H2,H3,K1 — migration + WS).

---

## DALGA 2-B DURUMU — İzolasyon/güvenlik (B-M1,B-M2,M4,EF-M2,EF-M3) DÜZELTİLDİ ✅ (2026-07-09)

| # | Fix | Dosya |
|---|-----|-------|
| B-M1 | isBlocked filtresi: public profil slug + sitemap + dizin doğrudan-id | public-profile.service.ts, company-connections.service.ts |
| B-M2 | İlan deliveryAddressId/billingAddressId sahiplik doğrulaması (assertListingAddressesOwned) create+update | company-listings.service.ts |
| M4 | listing-documents assertCanView blok kontrolü (CompanyBlocksService enjekte) + test | company-listing-documents.service.ts/.module.ts, make-docs-service.ts, listing-documents.spec.ts |
| EF-M2 | Profil update kategori ID validasyonu (CategoryService.validateIds minLevel:1) | company-profile.service.ts/.module.ts |
| EF-M3 | Şahıs firması (SOLE_PROPRIETOR) taxNumber maskesi (canSeeSensitive=false) | company-profile.service.ts |

Doğrulama: typecheck + build (5/5) + listing-documents (21, yeni blok testi), connections + company-profile (27) yeşil.

**Kalan Dalga 2:** altyapı (G-M1..M5, EF-M1), veri/şema (H1,H2,H3,K1 — migration).

---

## DALGA 2-A DURUMU — Para-yolu correctness (C4-C8) DÜZELTİLDİ ✅ (2026-07-09)

D3 (çift yönlü review) + 5 para-yolu MED uygulandı; typecheck + build (5/5) + para-yolu testleri (order-workflow, company-listings, company-orders, auction-modes = 64 test) yeşil.

| # | Fix | Dosya |
|---|-----|-------|
| C4 | paymentDecision'da sipariş CANCELLED/REJECTED ise ödeme kararı reddedilir | company-orders.service.ts |
| C5 | buyNow herhangi bir SUBMITTED teklifi (normal dahil) blokluyor — kilit baypası kapandı | company-listings.service.ts |
| C6 | publishListing closesAt (zorunlu+gelecek) + PRIVATE→davet≥1 doğrular | company-listings.service.ts |
| C7 | BuyNowDto (ISO8601 + validityDays 1-365) + geçmiş-tarih kontrolü | place-bid.dto.ts + controller/service |
| C8 | runFullAward/runItemAward tx-sonrası bildirim/realtime best-effort (rollback döngüsü kapandı) | company-listings.service.ts |

**Kalan Dalga 2:** izolasyon/güvenlik (B-M1,B-M2,M4,EF-M2,EF-M3), altyapı (G-M1..M5,EF-M1), veri/şema (H1,H2,H3,K1) — kullanıcı bu grubu seçmedi, bekliyor.

---

## DALGA 1 DURUMU — 8 HIGH DÜZELTİLDİ ✅ (2026-07-09)

Tüm HIGH'lar uygulandı; monorepo typecheck (8/8) + production build (5/5) + para-yolu/rol/auction API testleri (approvals, company-orders, order-workflow, company-listings, auction×4, roles-ownership = ~120 test) yeşil. satis-auction'daki tek düşüş truncateAll flake'i (izolasyonda 6/6).

| # | Fix | Dosya |
|---|-----|-------|
| G-H1 | EmailLog payload hassas tiplerde (password_reset/login_2fa/email_verify) maskeli | email.service.ts |
| EF-H1 | invite()'a assertCanGrantRoles eklendi | company-users.service.ts |
| C1 | Toplu award onay eşiği toTryAmount ile TRY normalize (+bid.currency select) | company-listings.service.ts |
| C2 | 5 koşulsuz status-yazma → updateMany+guard; requestApproval ilan-başına tek-PENDING | company-listings + company-approvals.service.ts |
| C3 | createNextRound auction allowedCurrencies=[primary] + primary-dışı carry engeli | company-listings.service.ts |
| FE-H1 | LogisticsDto'ya 5 eksik alan (district×2, loading/deliveryDate, stackable) | create-listing.dto.ts |
| FE-H2 | Onboarding ayrı teslimat adresi alanları + step validasyonu | onboarding-client.tsx |
| B-H1 | image/commit key prefix guard (+buildTenantProfilePrefix helper) | company-profile + storage.service.ts |

**B-H1 kalan (senin prod işin):** r2.dev public access'i kapat VEYA hassas belgeler için ayrı private bucket — kodla çözülemez.

**Yeni MED backlog (fix sırasında saptandı):** davet/referral e-posta token'ları da EmailLog.payload'da düz (hesap-ele-geçirme değil, yeni-hesap; resend meşru admin özelliği olduğu için redaksiyon dışı bırakıldı).

---

## YÖNETİCİ ÖZETİ

Zemin sağlam: build/typecheck/test yeşil (534→370 API testi 366 geçti, flake), auth temeli + kapalı-zarf REST yüzeyi + para değişmezleri + Prisma şeması SAĞLAM. Denetim 8 alanda 5 paralel denetçi + spot kod-teyidiyle yapıldı. **8 HIGH** (6'sı kod-teyitli), ~28 MED, ~30 LOW.

### HIGH tablosu (yayın öncesi zorunlu)
| # | Bulgu | Alan | Teyit |
|---|-------|------|-------|
| G-H1 | Parola-reset token + 2FA kodu EmailLog.payload'da DÜZ METİN → admin hesap ele geçirme | Altyapı | ✅ kod |
| EF-H1 | invite() rol-verme kontrolü atlıyor → yetki yükseltme (op-rol→admin davet) | Firma | ✅ kod |
| C1 | Tam kazandırma onay eşiği kur-körü → yabancı parayla onay baypası | Para | ✅ kod |
| C2 | Onay çevresinde koşulsuz listing-status yazma → AWARDED ezilip çift-sipariş | Para | ✅ kod |
| C3 | createNextRound RFQ→auction tek-para-birimi uygulanmıyor → karışık birim kıyas | Para | 2 ajan |
| FE-H1 | Lojistik ilan oluşturma tamamen kırık (FE↔BE alan uyumsuzluğu, 400) | Frontend | ✅ kod |
| FE-H2 | Onboarding'de ayrı teslimat adresi girilemiyor (fonksiyonel çıkmaz) | Frontend | ajan |
| B-H1* | R2 bucket public erişim (r2.dev) + presign prefix doğrulaması — tek bucket'ta KYC/teklif belgeleri | Altyapı | prod-teyit* |

*B-H1 gerçek etkisi prod R2 public/private ayarına bağlı (sandbox'tan doğrulanamadı).

### Düzeltme dalgası önerisi
- **Dalga 1 (HIGH, hepsi):** G-H1, EF-H1, C1, C2, C3, FE-H1, FE-H2 + B-H1 (config). Çoğu küçük, cerrahi fix.
- **Dalga 2 (MED — güvenlik/veri):** B-M1..M4, C4-C8, EF-M1-M3, G-M1-M5, H1-H4, D2.
- **Dalga 3 (MED/LOW — UX/tutarlılık + ürün kararları):** FE-M1..M6, D1/D3 (ürün kararı), kalan LOW'lar.

### Ürün kararı bekleyen (kod değil, senin çağrın)
- D1: Mesajlaşma ACTIVE bağlantı gerektirsin mi? (şu an bağlantısız firmaya mesaj serbest)
- D3: Review çift yön mü olsun (satıcı da alıcıyı puanlasın)?
- FE-H1: Lojistik district/tarih/stackable alanları isteniyor mu (BE'ye ekle) yoksa FE'den kaldırılsın mı?

---

## J.37 — Build/Test zemini ✅
- Typecheck: 34 TS hatası düzeltildi (dev seed scriptleri `seed-demo-fill.ts`, `add-anadolu-listing.ts` — noUncheckedIndexedAccess). Monorepo yeşil.
- Build: 5/5 paket OK.
- Web Vitest: 211/211 (33 dosya) ✅ · Admin Vitest: 46/46 (9 dosya) ✅
- API Jest: 366/370 geçti; 4 test / 5 suite başarısız = **FLAKE** (uzak Supabase pooler `truncateAll` beforeAll bağlantı düşmesi, 40 suite paralel yük). İzole/retry'de geçiyor (satis-auction 6/6). Kod regresyonu YOK.
  - BACKLOG (test-infra): `truncateAll` (`test/integration/test-db.ts:19`) bağlantı-retry wrapper'ı ile sarılmalı VEYA jest maxWorkers düşürülmeli → flake biter.

---

## B — Yetkilendirme & İzolasyon

Auth temeli SAĞLAM (JWT type doğrulama, DB'den taze companyId/roles/tier, tokenVersion). HIGH kod bulgusu yok; kapalı zarfın REST yüzeyi tam sağlam.

### H1 — R2 bucket public erişim + presign prefix doğrulaması
- `.env`: `R2_PUBLIC_BASE_URL=https://pub-*.r2.dev` tanımlı → dev public URL muhtemelen AÇIK. Tek bucket'ta KYC + teklif + sipariş belgeleri. Gizlilik yalnız "key bilinmiyor"a dayanıyor.
- `company-profile.service.ts:91` `image/commit` — istemci-verdiği key prefix/sahiplik doğrulanmadan presigned GET'e çevriliyor (diğer belge modülleri `company-*/{companyId}/` prefix'e pin'liyor).
- Fix: (a) production'da r2.dev public access kapat / hassas belgeler için ayrı private bucket veya hep presign; (b) `image/commit`'te `key.startsWith(buildTenantProfileKey(companyId,...))` doğrula.
- Not: sandbox'tan r2.dev erişilemedi (proxy), public açık mı kesin doğrulanamadı → prod'da manuel teyit.

### M1 — isBlocked (admin blok) görünürlük filtresi eksik (3 yer)
- `public-profile.service.ts:41` GET /public/companies/:slug — bloklu firma SEO profilinde görünür
- `public-profile.service.ts:62-72` sitemap — `isBlocked:false` yok
- `company-connections.service.ts:678` GET /company/directory/companies/:rothernId — bloklu firma profil+açık ihaleleri doğrudan-id ile görünür (arama filtreler, id yolu atlar)
- Fix: üç `where`'e `isBlocked:false` ekle (tek-satır fix'ler)

### M2 — İlan adres sahiplik doğrulaması yok → PII snapshot sızıntısı
- `company-listings.service.ts:830,1047` deliveryAddressId/billingAddressId dto'dan kontrolsüz; `orderDeliverySnapshot:2158` companyId filtresiz `findUnique` → award'da yabancı adres siparişe yazılır, karşı taraf görür.
- Fix: adres id'lerini `where:{id,companyId}` ile doğrula (teklif tarafı `resolveBidDeliveryAddress:2135` zaten yapıyor).

### M3/R1 — İlan sahibi mutasyonlarında rol + tier kapısı yok
- `company-listings.service.ts`: eliminate:4144, cancel:4222, closeBiddingEarly/changeClosingTime→ownerOpenListing:4459, closeNoAward:4417, updateInternalNotes:4397, publishListing:1186, updateListing:965, deleteListing:1164, createNextRound:3860, addInvitations:4011 — yalnız `companyId` eşitliği, rol yok. (create/placeBid/award rol YAPIYOR → tutarsız.)
- `updateListing:956` ayrıca `assertPaidForNewListingWork` çağırmıyor → STANDARD davet ekleyebilir.
- `buy:listing:manage`/`sell:listing:manage` izinleri katalogda var, hiç zorlanmıyor.
- Fix: bu mutasyonlara `assertHasPermission(buy/sell:listing:manage)` + update'e tier guard.

### R2 — permissionsOverride operasyonel serviste yok sayılıyor
- `create:752, placeBid:2275, buyNow:2943, award:3155, awardByItem:3435` ve `company-orders.service.ts:456 assertOrderRole` ham `user.roles.includes(...)` kontrolü yapıyor; `/me permissions` ve reports `permissionsOverride`'ı uyguluyor → drift. Kurucu'nun çıkardığı izin etkisiz.
- Fix: tek bir `hasEffectivePermission(user, perm)` helper'ı (override-farkında) tüm operasyonel kontrollerde kullan.

### M4/K2 — listing-documents blok kontrolü drift (İKİ denetçi + kod teyidi)
- `company-listing-documents.service.ts:38 assertCanView` — CompanyBlocksService enjekte edilmemiş; bağlantı/davet/ülke/tier bakıyor ama blok YOK. Ana getOne:1909/placeBid:2221 blok filtreliyor → engellenen firma şartname listeler+indirir.
- Fix: assertCanView'a `blockedCompanyIds` kontrolü ekle.

### K1 — WS oda aboneliği erişim kontrolsüz (kapalı zarf metadata sızıntısı)
- `realtime.gateway.ts:73-84 onSubscribe` herhangi bir company JWT'yi herhangi bir `listing:{id}`/`order:{id}` odasına alıyor. `company-listings.service.ts:2821` her placeBid'de pingListing → rakip, olay frekansından teklif sayısı/zamanlaması çıkarabilir.
- Fix: subscribe'da REST görünürlük kontrolünü uygula VEYA sahip-dışı ping'i yalnız ilgili `company:` odasına at.

### LOW (backlog)
- İlk temas blok kontrolü eksik (invite `company-connections:73`, mesaj `company-messages:241`) — sonraki adımlar korunuyor
- awardByItem 1-bit oracle (`company-listings:3453`) — belge var/yok sızıntısı
- DRAFT status kapısı yok (getOne:1935, listing-docs) — davetli DRAFT görebilir (cuid tahmin edilemez)
- 403/404 tutarsızlığı — PRIVATE varlık 403 ile doğrulanabilir
- `templates:manage` (3 şablon controller) + `buy:bid:review` zorlanmıyor (firma-içi)
- Sipariş belgesi yüklemede rol yok (taraf var) `company-order-documents:196`
- `GET /company/bank-accounts` okuma her role açık (muhtemelen kasıtlı — ŞÜPHELİ)
- listing-templates payload `@IsObject` only, 25MB — DoS-boyut

### Temiz doğrulanan
company-users/invitations (son-yönetici atomik FOR UPDATE), approvals (CAS), orders (banka accept companyId reddi:165, complete tam-ödeme), messages/inbox/blocks (composite key), connections (atomik CAS), notifications (userId scope + transactional-kapatılamaz), dashboard/reports (companyId scope + override-farkında), bid-documents (composite key + prefix pin), premium maskeleme (server-side: detail/teaserItems/auctionView), public-profile alan listesi (PII dönmüyor). Kapalı zarf REST yüzeyi: bidStats/rakip verisi hiçbir teklifçi yoluna sızmıyor.

### ŞÜPHELİ (doğrulanacak)
- Onay-event zinciri: requestApproval başlatıcı-scope doğrulaması okunmadı (runFullAward yeniden doğruluyor — savunma derinliği var)
- H1 gerçek etki: prod R2 public/private durumu manuel teyit

---

## C — Para Yolu (ilan/teklif/auction/award/sipariş/onay)

Çekirdek para değişmezleri SAĞLAM (aşağıda teyit listesi). 3 HIGH, 5 MED bulundu — hepsi 2 bağımsız denetçi + spot kod teyidiyle.

### C1 (HIGH) — Tam kazandırma onay eşiği KUR-KÖRÜ → yabancı parayla onay atlanır
- `company-listings.service.ts:3195-3196` (award) → `requestApproval`'a `amount: Number(bid.amount)` (teklifin KENDİ birimi) + `currency: primaryCurrency` (yanlış etiket) geçer. `company-approvals.service.ts:452` eşiği ham sayıyla kıyaslar (`input.amount < min`, min TRY bazlı).
- `awardByItem` DOĞRUSUNU yapıyor: `itemAwardTotal:3602` `toTryAmount` ile TRY'ye çevirip `currency:"TRY"` gönderiyor. Toplu award simetrisi bozuk.
- Örnek: eşik 100.000 TRY, 50.000 USD (~1.7M TRY) teklif → `50000 < 100000` → onay SKIPPED → **onay tamamen baypas** + kayıtta yanlış birim.
- Fix: toplu award'da da `toTryAmount(bid.amount, bid.currency)` + `currency:"TRY"`.

### C2 (HIGH) — Onay akışı çevresinde KOŞULSUZ listing status yazmaları → AWARDED ilanı ezip çift-sipariş
- Koşulsuz yazan 5 nokta: award pending `:3201`, awardByItem pending `:3479`, `onAwardRejected:3827`→CLOSED, `cancelRequest`(approvals:740)→CLOSED, `closeNoAward:4413`→CLOSED_NO_AWARD. Hepsi read-then-write, `updateMany+status guard` YOK.
- `requestApproval:430-500` ilan-başına tek-PENDING kısıtı YOK (yalnız APR-no unique). İki eşzamanlı award() → iki PENDING istek (~215ms round-trip'te pencere gerçek).
- Zincir: R1 onaylanır (Order + AWARDED) → bayat R2 reddedilir/iptal → `onAwardRejected`/`cancelRequest` koşulsuz CLOSED → sipariş dururken ilan CLOSED → sahip yeniden award → **ikinci Order**. (runFullAward'ın kendi updateMany guard'ı ikinci onayı bloklar ama reject/cancel yolu ilanı geri açar.)
- `closeNoAward` (MED alt-bulgu): eşzamanlı runFullAward AWARDED+Order yazdıktan sonra koşulsuz CLOSED_NO_AWARD ezer.
- Fix: bu 5 yazmayı `updateMany(where status in [beklenen]) + count kontrolü` ile koşulla + requestApproval'a ilan-başına tek-PENDING kısıtı.

### C3 (HIGH) — createNextRound RFQ→ENGLISH_AUCTION tek-para-birimi kuralını uygulamıyor
- `:3949-3980` yeni tur auction açarken `allowedCurrencies`/`primaryCurrency`'ye dokunmuyor (NextRoundDto'da alan yok). create/update auction'ı tek-birime zorlarken (`:852,1066`) tur geçişi zorlamıyor.
- Auction para mantığı HAM tutar: best `_min/_max:amount`, monotonluk/adım kıyası, `computeAuctionView` `orderBy:amount`. Karışık birimde 100 USD < 200 TRY sayılır. `carryBids:AUTO` önceki RFQ USD tekliflerini auction agregatına taşır.
- Fix: tur auction'a dönerken `allowedCurrencies=[primaryCurrency]` + primary-dışı teklifleri taşıma.

### C4 (MED) — İptal edilmiş siparişte ödeme ONAYLANABİLİYOR
- `company-orders.service.ts:630-719 paymentDecision` sipariş durumunu kontrol etmiyor (yalnız payment.status CAS). Alıcı ACCEPTED'da ödeme kaydeder → iptal eder (CANCELLED) → satıcı ödemeyi CONFIRMED yapar → iade akışı yokken CANCELLED sipariş üstünde onaylı para.
- Fix: paymentDecision'da sipariş CANCELLED/REJECTED ise reddet (veya CAS'a sipariş durumu ekle).

### C5 (MED) — buyNow, SUBMITTED normal teklifi ezebiliyor (KALEM'de kapsam daraltma)
- `:2982-2998` yalnız `isBuyNow` SUBMITTED'ı bloklar; normal SUBMITTED teklif buyNow ile üzerine yazılır (version++). KALEM modda `itemIds` alt-kümesiyle önceki kalemler silinir → kilitli teklifin kapsamı/toplamı düşürülebilir (SUBMITTED-kilit baypası).
- Fix: SUBMITTED normal teklif varken buyNow reddet veya kapsam daraltmayı engelle.

### C6 (MED) — publishListing tarih + PRIVATE-davet doğrulaması yapmıyor
- `:1180-1207`. (a) closesAt=null taslak publish → süresiz OPEN (cron kapatamaz, placeBid süre kontrolü atlanır). (b) closesAt geçmişte → publish anında cron CLOSED'a çeker. (c) PRIVATE+0 davet → kimsenin göremeyeceği OPEN ilan.
- Fix: publish'te closesAt zorunlu+gelecek + PRIVATE→davet≥1 (create'in non-draft yoluyla aynı kural).

### C7 (MED) — buyNow DTO validasyonu yok
- `controller:124-138` inline body, class-validator DTO yok. placeBid geçmiş teslim tarihini + validityDays 1-365 doğrularken buyNow denetimsiz (bozuk string → Invalid Date → Prisma 500).
- Fix: BuyNowDto (IsISO8601 + geçmiş-tarih + validityDays 1-365).

### C8 (MED) — decide() fail-closed rollback sipariş oluştuktan SONRA onayı geri açıyor
- `company-approvals.service.ts:665-693` emitAsync throw'da PENDING'e döner (varsayım: hiçbir şey uygulanmadı). Ama runFullAward'da atomik olan yalnız Order+durum tx'i; tx SONRASI `pushToCompany:3362`/DB okuması throw edebilir → Order var + AWARDED ama onay PENDING → onaycı tekrar dener → transition count=0 sonsuz döngü; iptal C2 üzerinden CLOSED'a düşürür.
- Fix: runFullAward/runItemAward tx-sonrası bildirim bölümü hatayı yutsun (fire-and-forget) veya rollback yalnız award-tx hatasında.

### LOW (backlog)
- eliminate+createNextRound AUTO: sahibin ELEDİĞİ teklifler taşımada SUBMITTED'a dirilir + `eliminationReason/eliminatedAt` sıfırlanmaz (bayat damga) — `:3894,3927,4163`
- Auction adım kontrolü yarışa açık (oku:2629 → yaz:2727 serileştirme yok) — `:2657`
- placeBid zaman-kontrolü ile yazma arası pencere (tx içinde re-check yok) — `:2300→2727`
- publish çift-tık (status guard yok) → çift bildirim — `:1189`
- ship/receive bekleyen-ödeme kapısı CAS dışında (para kaybı yok) — `orders:225,263`
- teminat silme + accept yarışı (satıcı teminatı silip accept edebilir) — `order-documents:131`
- legacy CREATED durumu accept kapılarını atlar (banka-snapshot'sız) — `schema:563`
- Kur boşken FALLBACK sabiti (USD=34) ile taban kıyası (yalnız warn) — `exchange-rate.service:33`
- estimateListingAmount ölü kod — `:1213`
- createNextRound RFQ turunda isSealedBid:false yazılır (create default true) — semantik drift `:3959`

---

## D — Sosyal Katman (bağlantı/blok/mesaj/bildirim/review/şikayet)

Blok, bildirim çift-kanal+transactional, review spoofing-korumaları, thread tekilliği SAĞLAM. 3 MED (2'si ürün-kararı ŞÜPHELİ), 3 LOW.

### D1 — KARARLAŞTIRILDI: mesajlaşma bağlantı GEREKTİRMEZ (kasıtlı, düzeltme YOK)
> 2026-07-09 ürün kararı: herkese açık ihalelerde firmalar bağlantı olmadan da iletişim kurmak zorunda kalabilir → mevcut davranış doğru. Yalnız kalan hardening: mesaj controller'ına `@Throttle` (spam) — LOW backlog.

### D1-eski (referans) — Mesajlaşma ACTIVE bağlantı ile kapılanmıyor
- `company-messages.service.ts:231-306 send` + `:168-228 getThread` yalnız: kendine-değil + firma-var + blok-yok kontrol ediyor. **ACTIVE bağlantı kontrolü YOK.** Herhangi bir firma, hedefin iç companyId'sini (cuid) bilerek bağlantısız firmaya thread açıp mesaj atabilir. Ayrıca mesaj controller'da `@Throttle` yok (yalnız global 100/dk) → spam vektörü.
- Memory modeli "ACTIVE olunca mesajlaşabilir" diyor → ihlal. AMA: alıcının ilan hakkında bağlanmadan satıcıya yazması meşru olabilir. **KARAR GEREK:** mesaj yalnız ACTIVE bağlantıda mı açık olmalı?

### D2 (MED) — Karşılıklı eşzamanlı bağlantı isteği → mükerrer PENDING/ACTIVE
- `CompanyConnection` unique kısıtı YÖNLÜ: `@@unique([inviterCompanyId, inviteeCompanyId])` (schema:497). A→B ve B→A ayrı anahtar. `createRequest:248` ters isteği atomik-olmayan findFirst (TOCTOU) ile yakalıyor; eşzamanlı ters-create + eşzamanlı accept → iki ACTIVE kayıt, list() çift gösterir.
- Fix: yön-bağımsız kısıt (uygulama seviyesi normalize edilmiş çift key veya accept'te ikinci-ACTIVE guard).

### D3 — KARARLAŞTIRILDI + UYGULANDI: review ÇİFT YÖNLÜ (2026-07-09)
> Karar: satıcı da alıcıyı puanlar. Uygulandı: schema `@@unique([orderId, reviewerCompanyId])` (migration `20260709102753_bidirectional_reviews`, dev'e db execute+resolve ile uygulandı), `company-reviews.service.ts` upsert alıcı VEYA satıcı (hedef siparişten türetilir, spoofing yok), FE sipariş detayında her iki tarafa kart (başlık perspektife göre). order-workflow 19/19 yeşil.

### D3-eski (referans) — Review tek yönlü (satıcı alıcıyı puanlayamaz)
- `company-reviews.service.ts:14-53` her zaman reviewer=buyer, target=seller; `orderId @unique` → sipariş başına tek review. Satıcı alıcıyı puanlayamaz. Kasıtlıysa OK. **KARAR GEREK:** çift yön mü olmalı?

### LOW (backlog)
- billingEmail e-posta kanalı kullanıcı pref'ini yok sayıyor (in-app uyguluyor) — `company-listings:97, company-orders:56` — tutarsız ama muhtemelen kasıtlı (firma adresi)
- resolveComplaint durum guard yok (RESOLVED tekrar resolve + tekrar suspend) — `admin-companies:483`
- Atomik olmayan silme yarışları yakalanmamış 500: `disconnect:866` (P2025), `send` upsert ilk mesaj (P2002) — nadir

### Teyit (SAĞLAM)
✓ Blok atomik+iki yön+bağlantı koparma `company-blocks:54` ✓ Bağlantı state machine (self/blok/ACTIVE→Conflict/atomik updateMany+guard) ✓ Thread tekilliği `@@unique(buyer,seller)` ✓ Mesajda dosya eki YOK (N/A) ✓ Bildirim çift-kanal + transactional enforce (password/referral/award/order kapatılamaz) + kategori çift-yön ✓ Review: yalnız COMPLETED, sipariş-başına-tek, order'dan türetilir (spoofing yok), 1-5, self-review imkansız ✓ Complaint self+mükerrer engeli, admin resolve audit-log'lu

---

## H — Veri Katmanı (Prisma schema/migration/seed) ✅ SAĞLIKLI

Migration↔schema TAM SENKRON (drift yok), tüm para Decimal (Float yok), `sectors`/`Supplier` borcu çözülmüş. Veri-kaybı riski aktif değil. 4 MED, 3 LOW.

### H1 (MED, latent) — Company→Order CASCADE, DB'de Restrict yok
- `schema.prisma:610-612` `CompanyOrder.buyer/sellerCompanyId` ikisi de ON DELETE CASCADE. Bir Company hard-delete edilse karşı taraf mevcut/ödemeli olsa bile tüm sipariş/ödeme/review silinir (finansal/hukuki kayıt kaybı).
- Realizm DÜŞÜK: `prisma.company.delete` app'te HİÇ çağrılmıyor (soft-delete: isActive/isBlocked/deletedAt). Risk yalnız ileride hard-delete eklenirse. Fix: onDelete Restrict veya soft-delete garantisini koru.
- ✓ İYİ: `CompanyOrder.listingId`→SET NULL (sipariş yetim kalmaz), `ListingBid.deliveryAddressId`→SET NULL.

### H2 (MED) — CompanyConnection yönlü unique → çift-yön ACTIVE (D2 ile çapraz-teyit)
- `schema.prisma:497` `@@unique([inviter, invitee])` yönlü; A→B ve B→A ayrı satır. Enforcement app'te (seed OR ile kontrol ediyor). D2 (sosyal) ile aynı bulgu. Fix: yön-bağımsız normalize key veya accept guard.
- ✓ `MessageThread @@unique(buyer,seller)` yönlü olması KASITLI-DOĞRU (alış/satış thread ayrı).

### H3 (MED) — CompanyOrder.currency String, diğerleri Currency enum
- `schema.prisma:617` `currency String @default("TRY")` iken ListingBid/ApprovalRequest/ExchangeRate `Currency` enum. Order geçersiz string tutabilir (app-tutarlı ama DB zayıf). Fix: Currency enum'a çevir (migration).

### H4 (MED) — seed demo firmalar prod-guard'sız sabit Demo1234!
- `seed.ts:194` ensureCompany 3 demo firmayı koşulsuz `Demo1234!` ile açıyor (NODE_ENV kontrolü yok; guard yalnız super-admin'de). Hafifletici: `package.json`'da `prisma.seed` yok → migrate deploy otomatik seed etmiyor, yalnız elle. Fix: demo firma oluşturmayı NODE_ENV!==production guard'ına al.

### LOW (backlog)
- `Listing.closesAt` index yok; cron `status+closesAt` sorguluyor (`scheduler:22`) — hacim büyürse `@@index([status,closesAt])` (C bloğu cron notuyla örtüşür)
- `Company.taxNumber` global unique (country-scope değil) — yurtdışı çakışma teorik, TR-only sorun değil
- AuditLog legacy `tenantId`/actorType isimlendirmesi (populate zaten bekleyen iş)

### Teyit (SAĞLAM)
✓ Migration↔schema senkron, tek squashed baseline, elle riskli SQL yok ✓ Order→Listing SET NULL ✓ Bid→Address SET NULL ✓ Para tümü Decimal (18,2/18,3/15,6) ✓ Kapalı-zarf unique [listingId,bidderCompanyId] + [bidId,questionId] + [currency,rateDate] ✓ sectors/Supplier borcu temiz ✓ seed plain-text parola yok (Supabase bridge) + super-admin prod guard

---

## E/F — Firma/Ayarlar/Admin

TCKN/IBAN algoritmaları, onboarding, son-yönetici garantisi, davet TTL, slug, döviz dayanıklılığı SAĞLAM. 1 HIGH, 3 MED.

### EF-H1 (HIGH) — invite() rol-verme kontrolünü atlıyor (yetki yükseltme) — KOD TEYİTLİ
- `company-users.service.ts:93 invite()` yalnız `assertValidRoleCombo` çağırıyor; `updateRoles:378`/`updateUser:417` ikisi de ayrıca `assertCanGrantRoles(actor, roles)` çağırıyor. Davet yolunda o guard YOK.
- `assertCanGrantRoles:620` YONETICI/ONAYLAYICI atamasını admin-olmayana yasaklar. Ön koşul: Kurucu bir op-rollü kullanıcıya `updatePermissions` override ile `users:manage` verirse → o admin-olmayan kullanıcı YONETICI DAVET EDEBİLİR → kabul edilince yeni admin doğar. Güncelleme yolunda kapalı olan senaryo davet yolunda açık.
- Fix: `invite()`'a `this.assertCanGrantRoles(actor, roles)` ekle (satır 101'den sonra).

### EF-M1 (MED) — Admin dashboard KPI'ları sayfa-limitli listeden (200 sonrası yanlış)
- `apps/admin/.../dashboard/page.tsx:24` "Toplam Firma"=`companies.length`, filter'lar bu dizide; kaynak `admin-companies.service.ts:117 take:200`, COUNT/groupBy yok. 200 firma sonrası KPI sabitlenir/eksik sayar. Fix: server-side `count()`/`groupBy`.

### EF-M2 (MED) — Profil güncellemede kategori ID doğrulanmıyor
- `company-profile.service.ts:154` buyer/sellerCategoryIds doğrudan yazılıyor (DTO yalnız IsString/ArrayMaxSize50). Onboarding + tender `validateIds` (var-olma+level) yaparken profil update yapmıyor. Geçersiz/aşırı-geniş kod → bildirim/keşif tutarsız. Fix: update'te de `CategoryService.validateIds`.

### EF-M3 (MED, ŞÜPHELİ) — Hassas maske taxNumber'ı atlıyor → şahıs firmasında TCKN sızıntısı
- `company-profile.service.ts:104` canSeeSensitive=false iken authorizedTckn/iban null'lanıyor ama `taxNumber` dönüyor. SOLE_PROPRIETOR'da taxNumber = 11 haneli TCKN → company:manage'siz üye (ör. SATISCI) kurucunun TCKN'sini görür (KVKK). Fix: şahıs firmasında taxNumber da maskele.

### LOW (backlog)
- Varsayılan banka/adres silinince yeni varsayılan terfi etmiyor + ilk kayıt oto-varsayılan değil (kozmetik; accept açık seçtiriyor) — `bank-accounts:72, addresses:83`
- audit-logs endpoint rol kapısı yok (tüm admin tipleri okur) `admin-audit.controller:6`; resolveComplaint transaction'sız + idempotent değil `admin-companies:483`

### Teyit (SAĞLAM)
✓ TCKN algoritması (`company-identity:26`) ✓ TR IBAN mod-97 (`:90`) ✓ Onboarding SAHİP-only+tek-seferlik+ülke-farkında, tek tx ✓ Son-yönetici FOR UPDATE atomik `:696` ✓ SAHİP devri tek-sahip zorunlu ✓ Davet 7g TTL+32-byte+tek-kullanım updateMany guard ✓ Kategori validateIds (var-olma+level) ✓ Adres silme aktif-ilan koruması+snapshot ✓ Banka accept companyId doğrulama ✓ Döviz 7g bayatlık+throttle+TCMB JPY normalizasyon+idempotent ✓ Audit append-only+log() throw-etmez ✓ Slug unique+TR latinize+NFKD

---

## G — Altyapı (email/webhook/storage/realtime/cron/health)

Fire-and-forget e-posta, webhook idempotency+imza fail-closed, realtime sinyal-only, TCMB dayanıklılık, Sentry PII-off SAĞLAM. 1 HIGH, 5 MED.

### G-H1 (HIGH) — Parola-reset token + 2FA/doğrulama kodu EmailLog.payload'da DÜZ METİN — KOD TEYİTLİ
- `email.service.ts:78` `payload: input.templateData.data` ham şablon verisini DB'ye yazıyor. `password-reset` resetUrl'de plain token, `company-auth.service.ts:206` `kodunuz: ${code}` düz kod içeriyor. Token/kod doğrulama için hash'li (`tokenHash`/`codeHash`) ama düz hali payload'da TTL boyunca (30-60dk).
- `admin-email-logs.service.ts:91 findOne` payload'u tam döndürüyor → herhangi bir admin (AdminJwtAuthGuard: SUPER_ADMIN/SALES/SUPPORT) veya DB-okuma erişimi olan biri aktif reset token/2FA kodu okuyup **herhangi bir kullanıcının hesabını ele geçirir**. Hash'lemenin amacı boşa çıkıyor. `resend()` de eski payload'la yeniden gönderir.
- Fix: hassas contextType'larda (password_reset, email_verify, login_2fa) payload'ı redakte ederek sakla (token/kod maskeli) — VEYA payload'a hiç yazma, yalnız render için tut.

### G-M1 (MED) — Cron'larda çift-çalışma/overlap koruması yok
- `listing.scheduler.ts:19 closeExpired` updateMany `{id:{in}}` — `status:"OPEN"` guard YOK, count kontrol yok → iki replica/overlap davetlilere ÇİFT kapanış e-postası. Aynı desen `sendClosingReminders:48`, `membership.scheduler:24` (çift downgrade), `approvals.scheduler` EVERY_MINUTE. Redis yok (mimari). Tek instance'ta latent, 2+ replica'da tetiklenir.
- Fix: koşullu `updateMany(where status=OPEN)` + count'a göre bildirim; ileride advisory lock. (C bloğu C2 deseniyle akraba: koşulsuz status yazımı.)

### G-M2 (MED) — Bounce/complaint sonrası suppression yok
- `resend-event.service.ts:195` bounce/complaint'i EmailLog.status işaretliyor ama BOUNCED hiçbir yerde okunmuyor → kalıcı-bounce adrese gönderim sürer (Resend itibar riski), kullanıcı uyarılmaz. Fix: hard-bounce'ta adresi geçersiz işaretle/uyar.

### G-M3 (MED) — Presigned PUT'ta boyut sınırı yok
- `storage.service.ts:119 generatePresignedPut` yalnız ContentType imzalı, `ContentLengthRange` yok → keyfi boyut yükleme (maliyet/DoS). Fix: ContentLengthRange ekle. (`../` path-traversal R2'da literal key, gerçek risk değil; sanitizeFilename yeterli.)

### G-M4 (MED) — Health endpoint'leri auth'suz + /health/storage iç bilgi sızdırıyor
- `health.controller.ts:60 GET storage` guard yok → public; bucket adı+envPrefix+R2 CORS döner + her çağrı R2'ya komut atar. `check()` DB ping timeout'suz (asılı kalabilir). Fix: storage health'i auth'la/kaldır, DB ping'e timeout.

### G-M5 (MED) — Resend webhook @SkipThrottle değil
- `resend-webhook.controller.ts:34` global Throttler'a tabi (100/60s/IP). Toplu gönderimde svix IP'sinden 100/dk aşılıp 429 → delivery-tracking gecikir (svix retry'ler, kalıcı kayıp yok). İmza koruduğu için `@SkipThrottle` uygun.

### LOW (backlog)
- Senkron gönderimde oto-retry yok (transient hata → kod hiç gitmez; reset caller yine success döner) `email.service.ts:103`
- App loglarında manuel PII (e-posta adresi) — Pino redaction yalnız HTTP alanları
- WS oda aboneliği existence/activity oracle (sinyal-only, veri yok) `realtime.gateway:73` — B/K1 ile aynı
- R2 dev/prod aynı bucket, envPrefix ayrımı; orphan nesne temizliği garanti değil

### Teyit (SAĞLAM)
✓ E-posta fire-and-forget (`void ...catch(logger)` — ana akış patlamaz, sessiz yutma yok) ✓ Render hatası FAILED+log ✓ EMAIL_FROM env-driven (onboarding@resend.dev yalnız dev) ✓ E-posta normalize ✓ Webhook imza fail-closed (secret yoksa prod 401, dev yalnız ALLOW_INSECURE) ✓ Webhook idempotent (eventId unique)+status precedence+atomik tx ✓ svix replay 5dk tolerans ✓ CSRF webhook muaf ✓ Realtime sinyal-only (payload yalnız id)+JWT handshake+oda-scoped+spam guard ✓ TCMB 15s timeout+cron-fail mevcut-kur+freshness degraded ✓ Cron TZ Europe/Istanbul ✓ Sentry sendDefaultPii:false+DSN yoksa no-op ✓ Boş catch{} kalıntısı YOK

---

## J.38 — Env/Secret Hijyeni ✅ (temiz)

- ✓ Kök `.env` gitignore'lı, git'te İZLENMİYOR (gerçek R2/Supabase kimlikleri güvende)
- ✓ `.env.example` + `.env.production.example` yalnız placeholder (`<PROJECT_REF>`, `<PASSWORD>`) — gerçek secret yok
- ✓ Kaynak kodda hardcoded secret YOK (sk_/re_/AKIA/JWT taraması boş)
- ✓ Prod seed guard: `seed.ts:145-155` NODE_ENV=production'da zayıf/örnek/<12-karakter INITIAL_ADMIN_PASSWORD → throw
- ✓ `.env.production.example` titiz: ALLOW_INSECURE_WEBHOOK yorumlu (prod kapalı), CORS domain-scoped, Sentry + Resend-from notları
- LOW: `apps/api/.env.test` git'te izleniyor ama içerik dummy (localhost + `test...` JWT) — sızıntı değil; gerçek test koşusu kök `.env`'den uzak Supabase alıyor (kafa karıştırıcı, secret değil)
- LOW: `CLAUDE.md` dev test parolaları (demo12345/admin12345/Test1234) içeriyor — dosya kendisi "repo public olursa kaldır" diye işaretlemiş; şimdilik private repo, kabul

## I — Frontend (web /company)

Genel kalite YÜKSEK: auth boundary flash'sız, tier/rol gating iki-katman (UI teaser + server sınır), query-key→realtime invalidation köprüsü, kapalı-zarf FE'de korunuyor. 2 HIGH (fonksiyonel), 6 MED.

### FE-H1 (HIGH) — Lojistik ilan oluşturma TAMAMEN KIRIK (FE↔BE alan uyumsuzluğu) — KOD TEYİTLİ
- `form-schema.ts:375` FE logistics objesi `originDistrict`/`destinationDistrict`/`loadingDate`/`deliveryDate`/`stackable` (+volumeM3) gönderiyor (boş string/false serialize olur). BE `LogisticsDto` (`create-listing.dto.ts:170`) bu alanları TANIMLAMIYOR. `main.ts:114-116` `whitelist+forbidNonWhitelisted` → nested obje reddedilir. UNSPSC "78…" (taşımacılık) kategorisi seçilince `isLogistics` otomatik açılır → o kategorideki HER ilan POST/PATCH'te 400 "property … should not exist".
- Fix: BE DTO'ya eksik 6 alanı ekle VEYA FE'yi DTO'ya hizala (district/date/stackable/volumeM3 kaldır veya BE'ye taşı). Ürün kararı: bu alanlar isteniyor mu?

### FE-H2 (HIGH, fonksiyonel çıkmaz) — Onboarding'de ayrı teslimat adresi girilemiyor
- `onboarding-client.tsx:300` tek `deliverySameAsBilling` checkbox; işaret kaldırılınca hiçbir teslimat input'u render edilmiyor, submit yalnız `false` bayrağı gönderiyor. BE `onboarding.dto.ts:84` deliveryCity/District/... tanımlı → farklı teslimat adresi asla girilemez. Hafifletici: varsayılan işaretli + Ayarlar→Adresler'den yönetilebilir. Fix: checkbox kalkınca teslimat adresi alanlarını render et.

### MED (backlog)
- FE-M1: interceptor "sessiz liste" `forgot-password` + `invitations` kapsamıyor → çift hata (inline kutu + toast) `company-auth/api.ts:76`
- FE-M2: teklif-ver onay diyaloğu "geri çekilebilir" diyor — kural #6 (Geri Çek kaldırıldı) ihlali, yanıltıcı metin `teklif-ver/page.tsx:1342`
- FE-M3: teklif-ver staged-dosya yolunda çift-submit penceresi (uploadDoc.isPending gate'te yok) `:1258`
- FE-M4: teklif-ver diyalog mutation başlamadan kapanıyor → "Gönderiliyor…" durumu ölü `:607`
- FE-M5: sayısal client validasyon BE sınırını yakalamıyor (validityDays>365, quantity ondalık, fiyat maxDecimal) → 400 `:470, form-schema:109`
- FE-M6: onboarding TR VKN/TCKN checksum client'ta doğrulanmıyor (`isValidTaxIdForCountry` import yok) `:82`

### LOW (backlog)
- Onboarding-öncesi panel flash (ŞÜPHELİ, nadir), `<Field error hint>` deseni teklif-ver'de yok, adres formu pending etiketi yok, kopya `?from=` sessiz düşme, a11y aria-live tutarsızlığı, foreign telefon sayımı uç durum, stale yorumlar

### Teyit (SAĞLAM)
✓ Auth boundary flash'sız (hydrate+user gelene kadar null) ✓ 401 interceptor clear+redirect ✓ public rotalar (authed) dışında ✓ Zustand persist httpOnly cookie (token store'da değil) ✓ Tier/rol iki-katman (PortalGuard+PremiumGate+PremiumOnly layout, server asıl sınır) ✓ Realtime→invalidation prefix-eşleşmeli ✓ Kapalı-zarf FE (rakip tutar render yok, SUBMITTED edit yok, geri-çek butonu yok) ✓ DTO alan adları PlaceBidDto ile birebir ✓ Wizard per-step trigger+tarih+SATIS/ALIM yönlendirme ✓ console.log/TODO/native alert YOK (ConfirmProvider)

## J.39 — Production Hazırlık (durum, backlog)

- Prod Supabase projesi HENÜZ açılmadı (memory: kullanıcı yapacak) — açılınca migrate:deploy + seed (güçlü INITIAL_ADMIN)
- Railway 3 servis (api/web/admin), RAILWAY_DOCKERFILE_PATH, Node≥22.18, NEXT_PUBLIC build-arg gotcha'ları memory'de kayıtlı
- İlk yayın = DEMO planlı
- BEKLEYEN altyapı (CLAUDE.md ile uyumlu): CSP/helmet, httpOnly cookie auth (V2), structured logger (Pino+redact), Sentry aktivasyonu, alert webhook, audit_logs populate
- Resend: prod domain doğrulaması (SPF+DKIM+DMARC) + onboarding@resend.dev → gerçek from adresi

---

## C — Teyit edilen kritik kurallar (SAĞLAM)
✓ Çift-award tx kilidi (`updateMany count guard` :3300,3679) ✓ Award anında bid SUBMITTED re-check :3255 ✓ Aynı kalem tek kazanan :3512 ✓ Order tutarı=bid snapshot (Decimal) :3318 ✓ Sipariş state machine atomik CAS :434 ✓ Teminat+banka accept kapısı (BEFORE_DELIVERY, companyId reddi) :144-169 ✓ complete() tam-ödeme kapısı (AWAITING reddi + CONFIRMED≥tutar) :318 ✓ recordPayment FOR UPDATE tavan :547 ✓ Onay CAS + fail-closed rollback :590,665 ✓ SUBMITTED kilidi (RFQ) :2314 ✓ WITHDRAWN diriltilemez :2309 ✓ Kendi ilanına teklif engeli :2213 ✓ version++/tek-aktif-teklif unique upsert :2728 ✓ Kalem kuralları (duplicate/requireAll/pozitif fiyat/soru) :2435 ✓ Auction monotonluk+adım Decimal :2657 ✓ Anti-sniping auto-extend :2791 ✓ SATIS taban kur-çevrimli :2538 ✓ Kur snapshot bid'e yazılır (para yolu canlı kura bağımsız) :2524 ✓ İlan/sipariş no Postgres sequence :1268,4475 ✓ createNextRound durum guard :3863 ✓ Cron süre-dolu OPEN→CLOSED :listing.scheduler.ts:19
